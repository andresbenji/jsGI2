//function that returns the smaller number between the two
function min(x, y) {
  if (x < y) {
    console.log(x);
  } else {
    console.log(y);
  }
}

min(12, 11); //output 11
min(55, 65); //output 55
